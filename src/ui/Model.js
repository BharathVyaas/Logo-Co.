/**
 * Model component represents a mobile navigation menu.
 *
 * @param {Object} props - Component properties.
 * @param {ReactNode} props.children - The content to be rendered inside the mobile menu.
 */
const Model = (props) => {
  // Render the mobile menu only when isClicked is true
  return props.children;
};

export default Model;
